<Window x:Class="SSHHelper.App.Views.LicenseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:SSHHelper.App.ViewModels"
        Title="SSHHelper - 授权验证" 
        Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Popupbox.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <materialDesign:Card Margin="20" UniformCornerRadius="8">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题 -->
            <TextBlock Grid.Row="0" 
                       Text="SSHHelper" 
                       FontSize="28" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"
                       Foreground="{DynamicResource PrimaryHueMidBrush}"/>

            <!-- 机器码 -->
            <Grid Grid.Row="1" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                           Text="机器码:" 
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                
                <TextBox Grid.Column="1" 
                         Text="{Binding MachineId, Mode=OneWay}"
                         IsReadOnly="True"
                         materialDesign:HintAssist.Hint="机器码"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                
                <Button Grid.Column="2"
                        Content="复制"
                        Command="{Binding CopyMachineIdCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="10,0,0,0"/>
            </Grid>

            <!-- 授权码 -->
            <TextBox Grid.Row="2"
                     Text="{Binding LicenseKey, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="请输入授权码"
                     materialDesign:HintAssist.HelperText="联系开发者获取授权码"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,20"
                     FontSize="16"
                     Padding="10"/>

            <!-- 验证按钮 -->
            <Button Grid.Row="3"
                    Content="验证授权"
                    Command="{Binding ValidateLicenseCommand}"
                    IsEnabled="{Binding IsValidButtonEnabled}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Height="45"
                    FontSize="16"
                    Margin="0,0,0,15"/>

            <!-- 进度条 -->
            <ProgressBar Grid.Row="4"
                         IsIndeterminate="{Binding IsValidating}"
                         Visibility="{Binding IsValidating, Converter={StaticResource BooleanToVisibilityConverter}}"
                         Height="4"
                         Margin="0,0,0,15"/>

            <!-- 状态消息 -->
            <TextBlock Grid.Row="5"
                       Text="{Binding ErrorMessage}"
                       Foreground="Red"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

            <!-- 成功消息 -->
            <StackPanel Grid.Row="6"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Visibility="{Binding ShowSuccess, Converter={StaticResource BooleanToVisibilityConverter}}">
                <materialDesign:PackIcon Kind="CheckCircle" 
                                        Foreground="Green" 
                                        Width="20" Height="20"
                                        VerticalAlignment="Center"
                                        Margin="0,0,5,0"/>
                <TextBlock Text="授权验证成功！" 
                          Foreground="Green"
                          VerticalAlignment="Center"/>
            </StackPanel>

            <!-- 窗口控制按钮 -->
            <Button Grid.Row="6"
                    Style="{StaticResource MaterialDesignIconButton}"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    ToolTip="关闭">
                <materialDesign:PackIcon Kind="Close"/>
            </Button>
        </Grid>
    </materialDesign:Card>
</Window>